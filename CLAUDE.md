@AGENTS.md

# Claude Code 전용 규칙

## 작업 공간과 배포

- 작업 시작 시 현재 Production 배포 ID/SHA와 `origin/main`을 확인합니다.
- 루트 체크아웃(`C:\Users\p\Desktop\park\furniblog`의 `main`)은 오래되고 미커밋 변경이 많습니다. **루트에서 checkout·pull·reset·배포하지 않습니다.** 작업은 `origin/main` 기준 worktree(`.worktrees/<이름>`)에서 합니다.
- 새 작업 전에 기존 worktree와 WIP 브랜치(`git worktree list`, `git branch -a`)를 확인하고, 이미 구현된 기능을 중복으로 만들지 않습니다.
- Production 배포와 `main` 머지는 대표님이 명시적으로 승인한 범위에서만 합니다.
- 규칙 문서(CLAUDE.md·AGENTS.md)를 main에 합친 뒤에는 루트 폴더의 같은 파일도 main 내용으로 덮어써서 갱신합니다.

## 진행 방식

- 한 번에 한 단계만 진행합니다. 단계가 끝나면 보고하고 멈춥니다. 여러 단계를 한 번의 승인으로 묶어 연속 진행하지 않습니다.
- 수정 전에는 항상 진단(현재 상태 확인)부터 하고, 진단 결과를 먼저 보고합니다.
- 대표님이 한 지시문에 여러 단계를 묶은 경우, 첫 단계 이후가 읽기 전용 진단이면 이어서 진행합니다. 수정·합치기·배포가 두 번 이상 포함되면 첫 단계 후 멈춥니다.
- 사소한 판단(문구·배치·표기 방식)은 합리적으로 정해 진행하고, 보고서에 결정 내용만 적습니다.

## 지시 검증과 멈춤

- 대표님 지시문은 채팅 Claude와 함께 작성되며 틀린 전제가 있을 수 있습니다. 실행 전에 실제 상태와 대조합니다.
- 다음 경우에는 실행하지 않고 멈춘 뒤 보고합니다: 전제가 실제와 다를 때 / 운영·데이터·다른 프로젝트에 영향이 생길 때 / 복구가 어려운 작업에 미확인 부분이 있을 때 / 범위가 넓어지거나 결과가 달라지는 방법이 필요할 때 / 지시끼리 또는 지시와 이 문서가 충돌할 때.
- 범위가 좁고 더 안전하며 결과가 같은 방법은 진행하되, 보고서에 "지시와 다르게 한 점"으로 명시합니다.
- 멈춤 보고 형식: 지시 내용 → 실제 확인한 사실 → 왜 문제인지 → 선택지 2~3개와 권장안.

## 데이터와 링크

- DB 가격·리뷰 수·Best for·치수·이미지는 출처 확인 전 사실이나 schema로 승격하지 않습니다.
- 존재 확인이 안 된 내부 URL로 링크하지 않습니다.
- DB 수정 전 백업은 저장소 밖 C:\Users\p\Desktop\park\chairpedia-backups\ 에 날짜_작업명 폴더로 저장합니다.

## 보고 방식

- 대표님은 비개발자입니다. 기술 내용은 쉬운 말로 설명합니다. 전문 용어를 쓸 때는 무슨 뜻인지 한 줄로 풀어 씁니다.
- 보고는 실제로 확인한 것, 문제, 바꾼 것, 검수 결과 순서로 씁니다. 사소한 문구 결정은 묻지 말고 합리적으로 정해서 진행합니다.
- **모든 보고의 마지막은 반드시 아래 세 부분으로 끝냅니다.** 해당 사항이 없으면 "없음"이라고 씁니다.
  1. **대표님이 결정할 것** — 선택지와 권장안을 함께, 한 줄로 답할 수 있게
  2. **대표님이 실행할 것** — 클릭·붙여넣기 단위로 구체적으로(어느 사이트, 어느 메뉴, 어떤 버튼, 붙여넣을 내용 전문)
  3. **Claude가 실행할 것** — 승인 후 Claude가 할 다음 작업
