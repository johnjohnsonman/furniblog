# 대량 사양 입력 충돌 방지

2026-09-20. 로컬 입력 도구 변경; 운영 DB 변경 없음.

## 발견 및 수정

같은 제품 slug에 지역 또는 옵션별로 다른 수치를 입력하면 SQL 생성기가 마지막 값을 채택하던 문제를 차단했다. 한 배치에서 같은 필드의 수치가 다르거나 고정 깊이와 조절식 깊이가 섞이면 오류로 종료한다. 검증 오류 시 기존 출력 SQL을 덮어쓰지 않는다. 서로 일치하는 복수 출처는 허용한다.

이는 한 입력 배치 안의 충돌 검사다. 기존 DB 값과의 충돌 또는 서로 다른 배치 사이의 구성 변경을 자동 해결하지 않는다. 현재 제품별 chair_specs 한 개 구조에서 옵션 범위를 합쳐서는 안 된다.

## 검증

`node scripts/test-fit-evidence-conflicts.cjs` 통과: 높이 충돌, 깊이 표현 충돌, 기존 출력 보존, 일치하는 복수 출처 허용.

## 다음 구현 범위

Zody의 Standard/Dual Posture 및 lumbar 유무처럼 값이 다른 제품은 구성별 사양 저장과 선택 규칙을 먼저 설계한다. 선택하지 않은 옵션의 범위를 합쳐 추천 점수를 높이지 않는다. 이 구조가 준비되기 전에는 기존 일반 제품에 임의의 옵션 값을 덮어쓰지 않는다.

대표님이 실행할 SQL은 없다. 추가 연구와 구성별 저장 구조는 아직 미완료다.

폴더: `C:\Users\p\Desktop\park\furniblog\.worktrees\chair-fit-engine\design-handoff`
