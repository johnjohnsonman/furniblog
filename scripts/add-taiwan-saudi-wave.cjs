const crypto=require("node:crypto"),fs=require("node:fs");
const path="content/showrooms/registry.json",registry=JSON.parse(fs.readFileSync(path,"utf8")),candidates=JSON.parse(fs.readFileSync("content/showrooms/taiwan-saudi-wave.json","utf8"));
const existing=new Set(registry.stores.map(x=>x.slug)),brands=new Map(registry.catalog.brands.map(x=>[x.name.toLowerCase(),x]));let added=0;const counts={};
for(const c of candidates){if(!c.valid||existing.has(c.slug))continue;const brand=c.brand?brands.get(c.brand.toLowerCase()):null;registry.stores.push({id:crypto.randomUUID(),slug:c.slug,name:c.name,status:"published",country_code:c.country_code,city:c.city,region:c.region,address:c.address,unit:"",latitude:c.latitude,longitude:c.longitude,timezone:c.timezone,phone:"",email:"",website_url:c.source_url,booking_url:"",store_type:"retailer",appointment:c.appointment,hours:{weekly:{},exceptions:{}},visit_notes:c.appointment==="required"?"Contact the showroom to book an appointment and confirm the chair you want to try before travelling.":"Contact the store before travelling to confirm chair availability and visit arrangements.",transport_notes:"",photos:[],source_url:c.source_url,checked_on:"2026-09-17",brands:brand?[{brand_id:brand.id,carried:"confirmed",official:"confirmed",source_url:c.source_url,checked_on:"2026-09-17"}]:[],models:[],updated_at:"2026-09-17T03:00:00.000Z"});existing.add(c.slug);counts[c.country_code]=(counts[c.country_code]||0)+1;added++;}
registry.checked_on="2026-09-17";fs.writeFileSync(path,JSON.stringify(registry,null,2)+"\n");console.log(`Added ${added}`,counts);
